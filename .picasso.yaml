version: 1

author: Sebastian Döll <sebastian@katallaxie.me>

project: Picasso

tasks:
  test:
    desc: teest
    vars:
      region:
        - test
    cmds:
      - go test -v ./...
  build:
    deps:
      - test
    vars:
      region:
        - test
    cmds:
      - go build
    watch:
      paths:
        - examples
      ignore:
        - .gitignore
        - .picasso.yml
    templates:
      - 
        file: ./examples/config.json.tpl
        output: ./config.json
        vars:
          foo: bar
      